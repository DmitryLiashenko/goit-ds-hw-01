[project]
name = "goit-ds-hw-01"
version = "0.1.0"
description = ""
authors = [
    {name = "DmitryLiashenko",email = "dmliash1@gmail.com"}
]
license = {text = "free"}
readme = "README.md"
requires-python = "3.12.*"
dependencies = [
    "python-telegram-bot (==20.7)",
    "python-dotenv (>=1.2.1,<2.0.0)",
    "google-api-python-client (==2.170.0)",
    "gspread (==6.2.0)",
    "oauth2client (==4.1.3)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
