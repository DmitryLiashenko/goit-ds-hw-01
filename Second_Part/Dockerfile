# 1) Базовый образ
FROM python:3.12-slim

# 2) Рабочая папка внутри контейнера
WORKDIR /app

# 3) Копируем исходники в контейнер
COPY . /app

# 4) По умолчанию запускаем bash, чтобы ты мог работать интерактивно
CMD ["/bin/bash"]